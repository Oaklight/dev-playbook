name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/lint-test.yml
    # Uses default python-versions: ["3.10", "3.11", "3.12", "3.13"]
